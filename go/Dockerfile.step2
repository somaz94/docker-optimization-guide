# Dockerfile.step2
FROM golang:1.24-alpine

WORKDIR /app

# Install git (may be needed for go mod download)
RUN apk add --no-cache git

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o main .

EXPOSE 8080
CMD ["./main"]
