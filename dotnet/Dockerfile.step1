# Dockerfile.step1
# Single stage using full SDK image (largest, includes build tools)
FROM mcr.microsoft.com/dotnet/sdk:9.0

WORKDIR /app

COPY . .
RUN dotnet publish -c Release -o out

EXPOSE 8080
CMD ["dotnet", "out/ProductApi.dll"]
